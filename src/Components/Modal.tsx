import React from "react";
import { useStore } from "../store";

const Modal = () => {
  const {isListModalOpen,isWorkspaceModalOpen, closeListModal, closeWorkspaceModal} = useStore();

  
  return <div></div>;
};

export default Modal;
